---
layout: distill
title: Practical Guide to Cell Segmentation, Domain Identification and Cell Type Annotation in Single-Cell Spatial Transcriptomics (Visium HD)
description: Example
tags: spatial-transcriptomics
date: 2026-03-14
featured: true
bibliography: 2026-03-14-distill.bib

authors:
  - name: Rafael Oliveira
    url: "https://www.linkedin.com/in/rafael-slo/"
    affiliations:
      name: IKOM, NTNU

# Optionally, you can add a table of contents to your post.
# NOTES:
#   - make sure that TOC names match the actual section names
#     for hyperlinks within the post to work correctly.
#   - we may want to automate TOC generation in the future using
#     jekyll-toc plugin (https://github.com/toshimaru/jekyll-toc).
toc:
  - name: Purpose of this tutorial
  - name: "Spatial Transcriptomics: A primer"
    subsections:
    - name: What does Spatial Transcriptomics unlock compared to other transcriptomics techniques?
    - name: Imaging-based Spatial Transcriptomics (iST)
    - name: Sequencing-based Spatial Transcriptomics (sST)
    - name: Shift from Deconvolution (spot based) to Cell Segmentation (bin based)
  - name: Exploring a Visium HD Dataset
    subsections:
    - name: Data Structure
    - name: Pre-processing
  - name: Cell Segmentation in Single‑Cell Spatial Transcriptomics using SOPA
    subsections:
    - name: StarDist
    - name: Proseg
  - name: Spatial Clustering and Domain Identification with Novae
  - name: Cell Type Annotation using Enrichmap and Cell Type Gene Set Signatures
  - name: Conclusion and Next Steps

# Below is an example of injecting additional post-specific styles.
# If you use this post as a template, delete this _styles block.
_styles: >
  .fake-img {
    background: #bbb;
    border: 1px solid rgba(0, 0, 0, 0.1);
    box-shadow: 0 0px 4px rgba(0, 0, 0, 0.1);
    margin-bottom: 12px;
  }
  .fake-img p {
    font-family: monospace;
    color: white;
    text-align: left;
    margin: 12px 0;
    text-align: center;
    font-size: 16px;
  }
---

## Purpose of this tutorial

TODO

---

## Spatial Transcriptomics: A primer

TODO


Add image from my ppt with the fruit analogy

### Imaging-based Spatial Transcriptomics (iST) vs Sequencing-based Spatial Transcriptomics (sST)

Spatial Transcriptomics (ST) technologies are generally classified into two primary categories: imaging-based and sequencing-based methods <d-cite key="Gay.2021"></d-cite>

Within the imaging-based category, technologies are further divided into in situ hybridization (ISH) and in situ sequencing (ISS)

Conversely, the sequencing-based category is primarily represented by in situ capturing (ISC) techniques

Imaging-based Spatial Transcriptomics (iST) identifies different mRNA species by targeting specific genes a priori using labeled complementary probes, which allows for the direct visualization of gene distribution within their native tissue context

Because of this targeted approach, imaging methods are often highly sensitive and capable of achieving subcellular or even single-molecule resolution

However, this reliance on specialized probes means that most imaging-based methods suffer from lower gene throughput and more limited gene panels than unbiased sequencing methods

Furthermore, as these techniques often require prior knowledge of the target genes to design the necessary labels, the total number of genes that can be measured simultaneously is restricted compared to transcriptome-wide approaches

Sequencing-based Spatial Transcriptomics (sST), primarily represented by in situ capturing (ISC), utilizes spatially barcoded arrays to capture polyadenylated RNA transcripts directly from tissue sections for analysis via next-generation sequencing (NGS) 

Unlike imaging-based methods, sST is unbiased and provides transcriptome-wide coverage, meaning no a priori knowledge of target genes is required; this makes it ideal for discovery-driven research and identifying novel biological mechanisms or rare transcripts

While early iterations like the standard Visium platform are limited by a resolution of 55 µm diameter spots—which typically encompass a mixture of multiple cells—newer high-definition technologies such as Visium HD have significantly advanced this resolution

Visium HD reduces the spot diameter to 2 µm and eliminates gaps between spots, enabling gap-free and bias-free profiling at single-cell scale resolution

Other high-resolution sequencing methods include Stereo-seq, which achieves nanoscale resolution using 220 nm DNA nanoballs, and Slide-seq, which utilizes 10 µm barcoded beads

Despite these advancements in resolution, ISC technologies generally encounter lower transcript capture efficiency compared to imaging-based methods, as they often rely on the vertical diffusion of transcripts onto a capture surface rather than direct in-tissue analysis

### What does Spatial Transcriptomics unlock compared to other transcriptomics techniques?


### Shift from Deconvolution (spot based) to Cell Segmentation (bin based)

TODO
Add image from my ppts

---

## Exploring a Visium HD Dataset

For this tuttorial we will use the Visium HD dataset from: 
https://www.10xgenomics.com/support/software/loupe-browser/latest/tutorials/assay-analysis/lb-hd-spatial-gene-expression
https://www.10xgenomics.com/datasets/visium-hd-cytassist-gene-expression-libraries-of-human-crc


The structure of this project should be as follows once you have downloaded the necessary data from the 10x website:

### Data Structure

{% highlight python linenos %}
spatial/
├── Microscopy/
│   ├── Visium_HD_Human_Colon_Cancer_image.tif
│   └── Visium_HD_Human_Colon_Cancer_tissue_image.btf
│
└── SpaceRanger/
    └── Visium_HD_Human_Colon_Cancer/
        └── outs/
            ├── binned_outputs/
            │   ├── square_002um/
            │   ├── square_008um/
            │   └── square_016um/
            │
            └── spatial/
                ├── aligned_fiducials.jpg
                ├── aligned_tissue_image.jpg
                ├── cytassist_image.tiff
                ├── detected_tissue_image.jpg
                ├── tissue_hires_image.png
                ├── tissue_lowres_image.png
                ├── Visium_HD_Human_Colon_Cancer_cloupe_008um.cloupe
                ├── Visium_HD_Human_Colon_Cancer_feature_slice.h5
                ├── Visium_HD_Human_Colon_Cancer_metrics_summary.csv
                ├── Visium_HD_Human_Colon_Cancer_probe_set.csv
                └── Visium_HD_Human_Colon_Cancer_slide_file.vtf
{% endhighlight %}

Check this [repository link](https://github.com/Rafael-Silva-Oliveira/segmentation-and-annotation.git) to find all the full script and structure necessary to follow this tutorial .

The imports look like this:

{% highlight python linenos %}
import warnings
from datetime import datetime
from pathlib import Path

import decoupler as dc
import enrichmap as em
import matplotlib as mpl
import matplotlib.pyplot as plt
import novae
import numpy as np
import pandas as pd
import scanpy as sc
import seaborn as sns
import sopa
import spatialdata as sd
import spatialdata_plot
import yaml
import zarr
from anndata import AnnData
from loguru import logger
import spatialdata_plot

warnings.filterwarnings(
    "ignore",
    message="Use `squidpy.pl.spatial_scatter`",
)

# Matplotlib defaults
mpl.rcParams["figure.dpi"] = 300
plt.style.use("bmh")
plt.rcParams.update(
    {
        "figure.figsize": (12, 8),
        "axes.facecolor": "white",
        "axes.edgecolor": "black",
    }
)
{% endhighlight %}

The following code is used to set up the directories and logging. Note that we will also be using the `config_crc_tutorial.yaml` to store and load the configuration parameters. 

{% highlight python linenos %}
PROJECT_DIR = Path(
    r"C:\Users\rafae\Projects\segmentation-and-annotation"
).resolve()
CONFIG_PATH = (
    PROJECT_DIR
    / "scripts"
    / "config_crc_tutorial.yaml"
)

with open(CONFIG_PATH, "r") as f:
    cfg = yaml.safe_load(f)

sample_id = cfg["samples"][0]["id"]
sample_name = cfg["samples"][0]["name"]
params = cfg["params"]
marker_genes = cfg["marker_genes"]

PROCESSED_DIR = (
    PROJECT_DIR
    / "data"
    / "spatial"
    / "processed"
).resolve()
FIGURES_DIR = (
    PROJECT_DIR / "figures"
).resolve()

run_name = f"crc_tutorial_{datetime.now().strftime('%d%m%Y_%H%M')}"
run_dir = (
    PROCESSED_DIR / run_name
).resolve()
run_dir.mkdir(
    parents=True, exist_ok=True
)
(run_dir / "figures").mkdir(
    exist_ok=True
)

logger.info(f"Run directory: {run_dir}")
logger.info(
    f"Sample: {sample_id} ({sample_name})"
)

{% endhighlight %}

### Reading the data

Once we have the downloaded files and in the project structure above, we can read the data using the `sopa` package <d-cite key="Blampey.2024"></d-cite> as follows:

{% highlight python linenos %}
logger.info(
    "=== STEP 1: Reading Visium HD data and running segmentation ==="
)

sdata = sopa.io.visium_hd(
    str(
        PROJECT_DIR
        / "data"
        / "spatial"
        / "SpaceRanger"
        / sample_id
        / "outs"
    ),
    dataset_id=sample_id,
    fullres_image_file=str(
        PROJECT_DIR
        / "data"
        / "spatial"
        / "Microscopy"
        / "Visium_HD_Human_Colon_Cancer_tissue_image.btf"
    ),
)


{% endhighlight %}

In order to speed things up, we will use a subset of the data. We can crop the dataset like this:
{% highlight python linenos %}
sdata_sub = sd.bounding_box_query(
    sdata,
    min_coordinate=[51000, 9000],
    max_coordinate=[56000, 14000],
    axes=("x", "y"),
    target_coordinate_system=sample_id,
    filter_table=True,
)

# Make the var_names unique, but not required for this tutorial
<!-- for (
    key,
    table,
) in sdata_sub.tables.items():
    table.var_names_make_unique() -->

<!-- sdata_sub.write("sdata_subset.zarr")  # save it -->

{% endhighlight %}

We can then plot the tissue image and the cropped image like this:
{% highlight python linenos %}
sdata_sub.pl.render_images(
    f"{sample_id}_full_image"
).pl.show(
    coordinate_systems=sample_id,
    figsize=(10, 10),
)

{% endhighlight %}

Which would look something like this:
{% include figure.liquid loading="eager" path="assets/img/segmentation_and_annotation/Visium_HD_Human_Colon_Cancer_full_res_cropped_v3.png" class="img-fluid rounded z-depth-1" %}


---

## Cell Segmentation in Single‑Cell Spatial Transcriptomics using SOPA

Now that we have the data loaded and cropped, we can start segmenting the cells. We will use the `sopa` package to do this. You can check it out in this [link](https://github.com/gustaveroussy/sopa).

{% highlight python linenos %}
# Patch-based cell segmentation
logger.info("Creating image patches...")
sopa.make_image_patches(sdata_sub)
{% endhighlight %}

### StarDist

Talk about stardist <d-cite key="Schmidt.2018"></d-cite>
{% highlight python linenos %}

logger.info(
    "Running StarDist segmentation..."
)
sopa.segmentation.stardist(
    sdata_sub, min_area=20
)
{% endhighlight %}
### Proseg

talk about proseg <d-cite key="Jones.2024b"></d-cite>
{% highlight python linenos %}

logger.info(
    "Running ProSeg refinement..."
)
sopa.segmentation.proseg(
    sdata_sub,
    prior_shapes_key="stardist_boundaries",
)
{% endhighlight %}

Once we have the segmentation, we can aggregate the gene expression to the cells (As seen in the `sopa` tutorial, this is mandatory if you used stardist only, but optional if you ran proseg)

{% highlight python linenos %}

logger.info(
    "Aggregating gene expression to cells..."
)
sopa.aggregate(
    sdata_sub,
    aggregate_channels=False,
    expand_radius_ratio=1,
)

{% endhighlight %}

{% highlight python linenos %}


{% endhighlight %}

{% highlight python linenos %}


{% endhighlight %}
TODO




{% highlight python linenos %}


{% endhighlight %}
TODO


{% highlight python linenos %}


{% endhighlight %}
TODO

---


### Pre-processing

TODO

Add code to remove sparse regions using connectivities (template code with existing images)



{% highlight python linenos %}


{% endhighlight %}


## Spatial Clustering and Domain Identification with Novae

TODO

---

## Cell Type Annotation using Enrichmap and Cell Type Gene Set Signatures

TODO

{% highlight python linenos %}


{% endhighlight %}
Mention gene set based vs reference scRNA based (highlight new implementation of famous rctd in python!! https://github.com/p-gueguen/rctd-py)
---

## Conclusion and Next Steps

TODO
